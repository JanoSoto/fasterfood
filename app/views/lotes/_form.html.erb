<%= simple_form_for(@lote) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.association :ingrediente %> 
    <%= f.input :fecha_hora_llegada %>
    <%= f.input :fecha_vencimiento %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
